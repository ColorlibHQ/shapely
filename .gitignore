# Dependencies
node_modules/
vendor/

# Build output
build/
shapely.zip

# Editors / OS
.idea/
.vscode/
.DS_Store
*.swp

# Compiled / generated
.sass-cache/
*.map

# package-lock.json is intentionally committed so `npm ci` installs the exact
# tree that was tested. phpcs.ruleset.xml is a real config used by linting.
# Both were previously ignored here by mistake.
